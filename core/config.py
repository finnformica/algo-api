class Settings:
    PROJECT_NAME: str = "Algo API"
    PROJECT_VERSION: str = "1.0.0"

class Responses:
    INVALID_TICKER: str = "Response invalid for ticker"

settings = Settings()
responses = Responses()